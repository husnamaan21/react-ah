import axios from 'axios'
import React,{useState,useEffect} from 'react'

export default function Effect() {
    const [api,setApi]=useState([])
   
    return (
        <div>
            {/* {

            api.map((i,v)=><h1 key={v}>{i.name}</h1>)

            } */}
            
        </div>
    )
}
